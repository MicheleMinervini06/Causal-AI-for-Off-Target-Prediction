_base: experiments/configs/base.yaml

experiment:
  name: exp_02_deep
  description: >
    Esperimento deep non ancora migrato completamente al nuovo approccio.
    Placeholder coerente con lo schema config di exp_01.

seed: 42

dataset:
  n_samples: 1200
  seq_len: 23

model:
  d_model: 32
  nhead: 4
  layers: 2

training:
  epochs: 8
  lr: 0.001
  batch_size: 32
