_base: experiments/exp_03_neural_scm/config_exp24_merged_split.yaml

experiment:
  name: Exp30_Ablation_EncoderBiologicalMismatch_UseEncoder
  tags: ["ablation", "encoder", "biological_mismatch", "positional_use_encoder", "chapter_4"]
  notes: "Ablation per Chapter 4 Sec 4.3: stesso setup di Exp24 ma positional_use_encoder=true. Il pos_node riceve 12-dim [mismatch_type(4) + sgRNA_base(4) + off_base(4)] da BiologicalMismatchEncoder invece del 4-dim mismatch-type ricalcolato internamente. Baseline per il confronto encoder ablation (Exp28 pairwise, Exp29 context_aware). Confronto con Exp24 isola il contributo di use_encoder."

model:
  encoder: "biological_mismatch"
  positional_use_encoder: true
  # Tutto il resto ereditato da Exp24:
  #   architecture=positional_mlp, embed_dim=12, hidden_dim=8,
  #   pam_mode=additive, hard_monotonicity=true,
  #   context_cols=[gc_sgRNA, gc_offtarget, concept_gc_delta]
